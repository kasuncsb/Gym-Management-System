'use client';

// Manager reports page — shares the same component as admin
import ReportsPage from '@/app/admin-dashboard/reports/page';

export default function ManagerReportsPage() {
    return <ReportsPage />;
}
