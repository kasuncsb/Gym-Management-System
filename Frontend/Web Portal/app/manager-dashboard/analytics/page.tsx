'use client';

// Manager analytics — shares the same component as admin
import AnalyticsPage from '@/app/admin-dashboard/analytics/page';

export default function ManagerAnalyticsPage() {
    return <AnalyticsPage />;
}
