'use client';

// Manager inventory page — shares the same component as admin
import InventoryPage from '@/app/admin-dashboard/inventory/page';

export default function ManagerInventoryPage() {
    return <InventoryPage />;
}
